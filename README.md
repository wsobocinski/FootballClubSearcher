# FootballClubSearcher
SpringBoot application for searching football clubs wikipedia urls.

### Application start at localhost:8080
### For search input query after adress (localhost:8080/{query})
### If football club for query is found, then its wiki page url and http status ok are returned
### If football club for query is not found, then "No results found" and http status not found are returned.
